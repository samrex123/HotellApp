@model HotelSite1.Models.Room

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Room</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Roomnum" class="control-label"></label>
                <input asp-for="Roomnum" class="form-control" />
                <span asp-validation-for="Roomnum" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Roomtypesid" class="control-label"></label>
                <select asp-for="Roomtypesid" class="form-control" asp-items="ViewBag.Roomtypesid"></select>
                <span asp-validation-for="Roomtypesid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
